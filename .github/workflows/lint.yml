name: Build and Lint

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - run: sudo apt-get update && sudo apt-get install -y yosys
          
      - run: yosys -p "read_verilog main/core/core.sv"
